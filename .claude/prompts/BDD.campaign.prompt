# Task

請你嚴格遵照「行為驅動開發 (BDD)」的方式，來完成 `docs/specs/campaign.feature` 中所有驗收情境的開發。 不可同時進行 BDD 開發流程中多個步驟也不能略過任何一步驟，必須一步一步扎實執行並確認每一步的結果。

# Context

## Existing Codebase

本次任務是在一個已有既有程式碼的專案上開發新功能，請先了解現有架構再動手。

**現有核心類別（`src/` 下）：**

|類別|檔案|職責|
|---|---|---|
|`Product`|`src/entities.py`|商品資料值物件，持有 `name`、`unit_price`、`category`|
|`OrderItem`|`src/entities.py`|訂單明細值物件，關聯一個 `Product` 並記錄 `quantity`|
|`Order`|`src/entities.py`|結帳結果值物件，持有明細清單、`original_amount`、`discount`、`total_amount`|
|`ThresholdDiscount`|`src/promotions.py`|促銷策略：滿額折扣|
|`BuyOneGetOnePromotion`|`src/promotions.py`|促銷策略：美妝買一送一|
|`OrderService`|`src/order_service.py`|結帳服務，接收 `OrderItem` 清單，套用所有促銷策略，回傳 `Order`|

**促銷策略介面（duck typing）：** 所有促銷類別實作相同的方法簽名：

```python
def apply(self, items: list[OrderItem], original_amount: float, current_discount: float) -> tuple[float, list[OrderItem]]:
    ...
```

新增促銷只需遵循此簽名，**不需要繼承任何基底類別**，也**不需要修改 `OrderService`**——其促銷迭代邏輯對所有符合介面的促銷類別通用。

**`OrderService.checkout()` 流程：**

1. 計算 `original_amount`：所有輸入明細的 `unit_price × quantity` 加總
2. 依序對每個促銷策略呼叫 `promotion.apply(items, original_amount, current_discount)`，取回更新後的 discount 與 items
3. `total_amount = original_amount - discount`
4. 回傳 `Order(result_items, original_amount, discount, total_amount)`

## New Feature

**功能：雙十一大量購買折扣（`DoubleElevenDiscount`）**

- 對訂單中每一種商品，以該商品的總購買數量計算：每滿 10 件享 8 折，不足 10 件的餘數以原價計算。
- 折扣以「商品名稱」為單位獨立計算，不同商品之間的數量不合併。
- 金額計算基準：折扣金額 = `(完整組數 × 10 × unit_price × 0.2)`，從 `current_discount` 累加。
- 此促銷**只修改折扣數字**，不修改商品明細清單（與 `ThresholdDiscount` 同型）。
- 對應 spec 檔案：`docs/specs/campaign.feature`（標記為 `@double_eleven`）

**Background 步驟說明：** `Given 雙十一優惠活動已啟動` 代表 `OrderService` 已注入 `DoubleElevenDiscount` 作為其促銷策略。

## Design Guideline

- 參考 entities ERD: `docs/design/ERD.png` 以及 OOD 設計圖：`docs/design/OOD.png`。兩張圖中所指示的類別屬性及操作只是基準，你可視情況增加新的類別、屬性或行為。
- 新的促銷類別新增至 `src/promotions.py`，**不需要修改 `OrderService`**。

## Tech Stack

1. Language Env：Python 3.11+
2. BDD Test framework: Behave
3. Native test framework: pytest

## Application Environment

1. App 類型：純模組程式碼
2. Source code root: `src/`
3. Feature files 位置: `features/`
4. Step definitions 位置: `features/steps/`
5. 既有 step definitions 位於 `features/steps/order_steps.py`；新的 steps 可新增於同檔案或獨立的新檔案

# BDD 開發流程

1. 先閱讀並理解既有的 `features/order.feature` 與 `features/steps/order_steps.py`，確認現有 step 定義的用字，在撰寫新 steps 時優先複用，避免重複定義相同語意的步驟。確認既有測試全數通過後，再繼續。
    
2. 建置 walking skeleton：在 `features/` 下為新功能建立對應的 feature file（內容來自 `docs/specs/campaign.feature`），並確認 behave 可以識別並執行到至少一個 scenario（可先全數標記 `@skip` 僅留一個驗證框架可運行）。
    
3. 嚴格遵守 BDD 以及最小增量原則來開發所有程式碼，針對所有 scenario，一次開發一個 scenario，依序進行：
    
    A. 一次選擇一個 scenario 實作，除此 scenario 之外的測試全部都用 `@skip` 標記忽略。撰寫此 scenario 對應的 steps（given、when、then）、開啟相關類別，但是每個類別的行為都不實作，並且執行測試，確認測試失敗（test fail），且失敗原因並非框架層級的錯誤，而是期望的「值」上的錯誤。嚴格確認這步驟完成後才能進行下一步的實作。
    
    B. 為了通過上一步所撰寫的測試程式碼，請實作相關類別所需的程式碼，並確認能讓所有測試程式碼都通過。請嚴格確認有執行到測試程式碼，從 test report 中覆述一次目前 test passed 的數量。
    
    C. 遵守 clean code 原則，思考是否要重構每個類別的內部程式碼，如果必要重構的話，在重構完成之後，再執行一次測試，確保所有測試仍然通過，否則需修正邏輯直到測試全數通過。
    
4. 所有 scenario 開發完畢後，移除全部 `@skip` 標記，執行完整測試套件（含既有的 `order.feature`），確認所有測試全數通過，並回報最終 test report。
    
